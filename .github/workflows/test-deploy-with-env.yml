name: Deploy with environment

on:
  workflow_dispatch:
  push:
#  pull_request:
#    branches:
#      - dev
#      - release/**
#      - hotfix/**

jobs:
  determine-environment:
    name: env
    runs-on: ubuntu-latest
    steps:
      - name: Some check on branch
        id: determine-environment
        run: |
          echo "Running on branch ${{ github.ref }}"
          if [ "${{ github.ref }}" = "refs/heads/main" ]; then
            echo "env_name=prod" >> $GITHUB_ENV
          else
            echo "env_name=dev" >> $GITHUB_ENV
          fi
      - name: Check if required variables are configured in environment
        if: ${{ vars.HOST == '' }}
        run: echo "Variable 'HOST' does not exist in environment '${{ env.env_name }}'" && exit 1
      - name: Check if required secrets are configured in environment
        if: ${{ vars.SSH_PRIVATE_KEY == '' }}
        run: echo "Variable 'SSH_PRIVATE_KEY' does not exist in environment '${{ env.env_name }}'" && exit 1

  scp-docker-compose:
    name: Secure copy docker-compose from ${{ env.env_name }} to ${{ vars.host }}
    needs: determine-environment
    environment: ${{ needs.determine-environment.env.env_name }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Copy docker-compose file via SCP
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ vars.HOST }}
          username: root
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: docker-compose.yml
          target: /root

  deploy-docker-compose:
    name: Deploy docker-compose via SSH
    needs: [scp-docker-compose, determine-environment]
    environment: ${{ env.env_name }}
    runs-on: ubuntu-latest
    steps:
      - name: SSH deployment with docker-compose
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ vars.HOST }}
          username: root
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            docker system prune --all --force
            DOCKER_COMPOSE_PATH=/root/docker-compose.yml
            docker-compose -f $DOCKER_COMPOSE_PATH pull
            docker-compose -f $DOCKER_COMPOSE_PATH up -d --force-recreate